{
  "name": "trello-clone-server",
  "version": "1.0.0",
  "description": "Trello Clone Backend API",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "seed": "node seed.js",
    "schema": "node -e \"require('./db'); const fs = require('fs'); const pool = require('./db'); const sql = fs.readFileSync('./schema.sql', 'utf8'); pool.query(sql).then(() => { console.log('Schema created'); process.exit(0); }).catch(e => { console.error(e); process.exit(1); })\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
